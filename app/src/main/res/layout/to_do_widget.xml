<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Widget.ToDoCreator.AppWidget.Container"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:theme="@style/Theme.ToDoCreator.AppWidgetContainer">

    <TextView
        android:id="@+id/appwidget_text"
        style="@style/Widget.ToDoCreator.AppWidget.InnerView"
        android:layout_width="250dp"
        android:layout_height="185dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="8dp"
        android:contentDescription="Keine ToDos vorhanden"
        android:text="Keine ToDos vorhanden"
        android:textSize="24sp"
        android:textStyle="bold" />

</RelativeLayout>